# Railway's healthcheckPath accepts only letters, digits, "/" and "_", so the
# real probe path (/1.0/healthcheck) is unusable. Tomcat's RewriteValve runs
# before the servlet chain, so /healthz reaches the same handler.
RewriteRule ^/healthz$ /1.0/healthcheck [L]
